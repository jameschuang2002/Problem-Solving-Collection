/* Question Description 
 * Same as backpack with bag max at 10 ^ 9 */

#include <stdio.h>
#include <stdlib.h>

int main(void){
	
	/* input and variable declaration */
	int n;
	scanf("%d", &n);
	int w[n], v[n];
	for(int i = 0; i < n; i++)
		scanf("%d%d", &w[i], &v[i]);
	int W;
	scanf("%d", &W);
	
	
} 
